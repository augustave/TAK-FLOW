# EW Degradation Test Suite (v1)

> **Evidence provenance (added 2026-07-03).** Ingested into version control from a
> previously untracked repo-root artifact cited by claim C-012. Reproducibility status
> per test case:
> - **EW-001 (alpha-decay): partially re-runnable.** `npm run test:ew` asserts strictly
>   decreasing confidence on an in-zone track every sample; the exact 0.01/sec rate is
>   not separately asserted by the automated lane.
> - **EW-002 (culling): re-runnable** via `npm run test:ew`. Note the worker culls on
>   `confidence <= 0.05` **or** uncertainty radius > 20 km; the automated lane typically
>   exercises the radius path, which fires first for non-UUV tracks pinned in-zone.
> - **EW-003 (ghost zero-trust): re-runnable** via `npm run test:ew` (ghost-track
>   isolation and designation blocking test).

## Executive Summary
This document outlines the formal Electronic Warfare (EW) degradation test cases for the TAK-FLOW project. These tests verify the system's ability to maintain a zero-trust posture while correctly identifying and culling low-confidence tracks under EMCON conditions.

## Test Cases

### EW-001: Alpha-Decay Verification
- **Intent:** Verify that tracks within an active EW zone exhibit linear confidence decay over time.
- **Requirement:** `aConfidence` must decrease by 0.01 per second during EMCON periods.
- **Pass Criteria:** Measured decay rate of exactly 0.01/sec until it reaches the culling threshold.

### EW-002: Track Culling Threshold
- **Intent:** Verify that tracks are automatically culled from the system state when their confidence reaches the EMCON limit.
- **Requirement:** Tracks with `aConfidence` <= 0.05 must be removed from the active track buffer.
- **Pass Criteria:** Successful removal of the track ID from the `TrackManager` state and UI.

### EW-003: Ghost-Track Zero-Trust Posture
- **Intent:** Verify that injected ghost tracks are isolated from the strike-designation workflow.
- **Requirement:** All tracks with confidence < 0.5 must be blocked from initiating a designation.
- **Pass Criteria:** UI alert `INSUFFICIENT TRACK PROVENANCE` triggered upon attempted designation.

## Results
- **EW-001:** PASS
- **EW-002:** PASS
- **EW-003:** PASS

---
*Generated by TAK-FLOW Automation & Verification Engine*
